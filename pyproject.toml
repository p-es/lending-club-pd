[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "lending_pd"
version = "0.1.0"
description = "Probability-of-default modelling on LendingClub loans"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

